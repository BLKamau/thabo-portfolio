// Convenience index for the analytics package.
// Consumers import from here; the transport format can change underneath.

export { initAnalytics, track, flush, getQueue } from "./log";
export { initWebVitals } from "./webVitals";
